{% include "_header.j2" %}
server {
    listen {{ N_port | default(80) }};
    server_name {{ S_domain }};

{% block location %}
    location / {
        proxy_pass http://127.0.0.1:{{ N_port | default(80) }};
    }
{% endblock %}
}
